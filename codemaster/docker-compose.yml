version: "3.9"

services:
  frontend:
    build: .
    container_name: codementor-frontend
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    environment:
      NODE_ENV: development